# 无人机飞手服务平台部署文档

欢迎使用无人机飞手服务平台！本部署文档旨在帮助您轻松完成小程序的部署，即使您是初学者，也能按照步骤一步步完成。

## 第1部分：环境准备与开通

在开始之前，请确保您已经拥有一个微信账号。

### 1.1 注册微信小程序账号

1.  打开微信公众平台官方网站：[https://mp.weixin.qq.com/](https://mp.weixin.qq.com/)
2.  点击右上角的“立即注册”按钮。
3.  选择注册账号的类型为“小程序”。
4.  按照页面提示，填写您的邮箱、密码等信息，并完成邮箱验证。
5.  完成信息登记。根据您的主体类型（个人或企业）填写相应的信息。对于个人开发者，通常需要完成管理员的微信扫码验证。
    > **注意**：个人主体的小程序在某些API（如支付）上会有限制，但对于本项目的基础功能是足够的。

### 1.2 获取小程序的 AppID

注册成功并登录后，您将进入小程序管理后台。

1.  在左侧菜单栏中，点击“开发” -> “开发管理”。
2.  在“开发者ID”区域，您会看到小程序的 **AppID(小程序ID)**。这是一个以 `wx` 开头的字符串。
3.  **请务必复制并妥善保管这个 AppID**，我们在后续配置开发工具时会用到它。

    ![截图示意：在微信后台找到AppID的位置](https://res.wx.qq.com/a/wx_fed/wechat_open_doc/20210729/1.4.15/developer/assets/4-1.11451f7a.png) *(这是一个示例图，请以您自己后台的界面为准)*

### 1.3 开通云开发环境

微信云开发为我们提供了后端即服务（BaaS），无需自己搭建服务器。

1.  登录小程序管理后台。
2.  在左侧菜单栏中，找到并点击“云开发”菜单。
3.  如果您是首次使用，会看到一个开通云开发的引导页面。点击“立即开通”。
4.  系统会提示您创建一个新的云开发环境。您需要为这个环境设置一个**环境名称**（例如 `drone-prod`），并选择一个付费套餐。对于初学者和测试阶段，**“按量计费”模式** 是一个很好的选择，它有免费的资源额度。
5.  点击“创建”，系统会自动为您配置云环境。这个过程可能需要几分钟。
6.  环境创建成功后，您会看到一个**环境 ID**。通常格式为 `环境名称-xxxxxx`。
7.  **请务必复制并妥善保管这个环境 ID**，我们稍后也会用到。

至此，您的线上环境已经准备就绪！我们拥有了小程序的“身份证”（AppID）和它的“云端大脑”（云开发环境ID）。

---
*接下来，我们将介绍如何下载和配置微信开发者工具。*

## 第2部分：开发者工具配置与代码导入

微信开发者工具是开发、调试和上传小程序代码的官方IDE。

### 2.1 下载并安装开发者工具

1.  访问微信开发者工具下载页面：[https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)
2.  根据您的操作系统（Windows 或 macOS）下载对应的稳定版（Stable Build）。
3.  下载完成后，像安装普通软件一样进行安装。

### 2.2 导入项目

1.  打开安装好的微信开发者工具。
2.  使用您的微信扫码登录。请确保这个微信是您注册小程序时绑定的管理员微信。
3.  在启动页面，选择“导入”。
4.  在导入项目设置页面，进行如下配置：
    *   **项目名称**：可以自定义，例如“无人机飞手服务平台”。
    *   **目录**：点击选择按钮，**选择您存放我们项目代码的 `miniprogram` 文件夹**。
    *   **AppID**：**粘贴您在 1.2 节中获取的 AppID**。
    *   **开发模式**：选择“小程序”。
    *   **后端服务**：选择“微信云开发”。
5.  点击“导入”按钮。

### 2.3 关联云开发环境

项目成功导入后，开发者工具会自动识别出这是一个云开发项目。

1.  在开发者工具顶部的工具栏中，找到并点击“云开发”按钮。这将打开云开发控制台。
2.  在弹出的云开发控制台窗口中，它可能会提示您“当前未选择环境”。
3.  点击“设置”按钮（或环境选择下拉菜单），在列表中应该能看到您在 1.3 节中创建的云环境。
4.  选择您的云开发环境。开发者工具会自动将其与当前项目关联。
5.  关联成功后，您可以在云开发控制台中看到“数据库”、“存储”、“云函数”等管理标签页。

至此，您的本地开发环境已经和云端环境成功连接。您可以开始进行后续的代码部署和调试了。

---
*接下来，我们将部署数据库和云函数。*

## 第3部分：后端部署：数据库与云函数

现在，我们需要将应用的“骨架”（数据库集合）和“神经系统”（云函数）部署到云开发环境中。

### 3.1 创建数据库集合

我们的应用需要几个数据库集合来存储数据。您需要手动创建它们。

1.  在微信开发者工具中，打开“云开发控制台”。
2.  切换到“数据库”标签页。
3.  点击“集合名称”旁边的“+”号，开始创建新的集合。
4.  根据下面的列表，依次创建所有需要的集合。**集合名称必须完全匹配，区分大小写。**

    *   `Users`
    *   `Authentications`
    *   `Tasks`
    *   `Orders`
    *   `News`

5.  **（可选，推荐）为集合创建索引**：为了提高查询效率，我们可以为某些字段创建索引。
    *   点击进入 `Users` 集合，切换到“索引管理”标签页，点击“添加索引”。
    *   **索引字段**填 `_openid`，**索引方式**选择“唯一”，点击确定。这将确保每个微信用户在 `Users` 集合中只有一条记录。
    *   用同样的方式，为 `Authentications` 集合的 `userId` 字段和 `Tasks` 集合的 `publisherId` 字段也创建**非唯一**索引。

### 3.2 部署云函数

云函数是我们所有业务逻辑的载体。您需要将项目 `cloudfunctions` 目录下的所有函数都部署到云端。

1.  在微信开发者工具左侧的目录树中，找到 `cloudfunctions` 文件夹。
2.  右键点击第一个云函数文件夹（例如 `auth`）。
3.  在弹出的菜单中，选择“**上传并部署：云端安装依赖**”。这是一个非常方便的选项，它会自动在云端处理函数的依赖库。
4.  开发者工具会开始部署过程，您可以在控制台（Console）中看到部署进度。
5.  **对 `cloudfunctions` 目录下的每一个文件夹重复此操作**，直到以下所有云函数都部署成功：
    *   `auth`
    *   `admin`
    *   `tasks`
    *   `orders`
    *   `news`

    > **提示**：当您右键点击某个云函数文件夹时，如果看到“创建并部署”而不是“上传并部署”，说明云端还没有这个函数，效果是一样的。

部署成功后，您可以在云开发控制台的“云函数”标签页中看到您刚刚上传的所有函数。

至此，您的后端服务已经完全部署好了！数据库已经准备好接收数据，云函数也准备好处理各种业务请求。

---
*最后，我们将进行小程序的最终配置并上传发布。*

## 第4部分：小程序配置、上传与后台操作指南

万事俱备，只欠东风。现在我们来完成最后的步骤。

### 4.1 配置小程序云环境

小程序代码需要知道它应该连接到哪个云开发环境。

1.  在微信开发者工具左侧的目录树中，找到并打开 `miniprogram/app.js` 文件。
2.  在文件的 `onLaunch` 函数中，找到如下代码块：
    ```javascript
    wx.cloud.init({
      // env: 'YOUR_ENV_ID', // <--- 在这里修改
      traceUser: true,
    });
    ```
3.  **将 `'YOUR_ENV_ID'` 替换为您在 1.3 节中获取的云开发环境 ID**。
4.  按 `Ctrl+S` 或 `Cmd+S` 保存文件。

### 4.2 编译并预览

在上传之前，我们先在本地进行最后检查。

1.  点击开发者工具顶部工具栏中的“编译”按钮。
2.  工具左侧的模拟器将开始运行我们的小程序。您可以像在手机上一样操作，测试登录、浏览等基本功能是否正常。
3.  点击“预览”按钮，用您的手机微信扫描弹出的二维码，可以在真实设备上体验，效果更佳。

### 4.3 上传代码并发布

确认预览无误后，就可以上传代码了。

1.  点击开发者工具右上角的“上传”按钮。
2.  在弹出的窗口中，填写一个**版本号**（例如 `1.0.0`）和**项目备注**（例如“首次提交”）。
3.  点击“上传”。上传成功后会得到提示。
4.  现在，回到**微信小程序管理后台** ([https://mp.weixin.qq.com/](https://mp.weixin.qq.com/))。
5.  在左侧菜单栏中，选择“管理” -> “版本管理”。
6.  在“开发版本”中，您应该能看到刚刚上传的版本。
7.  点击“提交审核”，按照提示填写一些基本信息，然后提交给微信官方审核。
8.  审核通过后，回到该页面，点击“提交发布”，您的无人机飞手服务平台小程序就正式上线啦！

## 附录：后台操作简易指南

### A.1 如何成为管理员

1.  在微信开发者工具中，打开云开发控制台 -> 数据库 -> `Users` 集合。
2.  找到您自己的那条用户记录（可以通过 `_openid` 或微信昵称识别）。
3.  双击您的记录进行编辑，找到 `role` 字段。
4.  将 `role` 字段的值修改为 `["customer", "admin"]` (这是一个数组)。
5.  保存修改。现在，您就拥有了管理员权限。

### A.2 如何审核认证

1.  使用管理员账号登录小程序后台（目前后台入口与小程序端集成）。
2.  进入“管理中心” -> “认证审核”。
3.  您会看到所有待审核的列表。点击任意一条进入详情页。
4.  在详情页，您可以查看用户提交的所有信息（包括图片）。
5.  根据审核结果，点击“批准”或“拒绝”按钮。如果拒绝，系统会提示您填写拒绝原因。

### A.3 如何发布新闻

1.  进入“管理中心” -> “新闻管理”。
2.  点击“发布新文章”按钮。
3.  填写文章的标题、内容、选择分类、上传封面图等。
4.  点击“发布”，文章就会出现在小程序的新闻资讯模块中。

---
**部署完成，恭喜！** 如果遇到任何问题，请回顾文档中的每一步操作。
